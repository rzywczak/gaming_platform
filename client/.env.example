# Leave empty when the React build is served by the same Node server.
# Use a full URL only when the API is hosted separately, e.g. https://api.example.com
REACT_APP_API_URL=

# Leave empty for same-origin Socket.IO.
# Use the backend origin only when the socket server is hosted separately.
REACT_APP_SOCKET_URL=
